// This file is intentionally left blank as the Pricing page has been removed in favor of the Quotation section.

import QuotationSection from '../components/QuotationSection';

export default function Pricing({ darkMode }) {
  return <QuotationSection darkMode={darkMode} />;
}
